var quadrant = function(a, b, c)
var delta = (b*b) - (4*a*c)
var x;
var x1;
var x2;
if (delta == 0){
     console.log x("this question has one solution" , -b / (2*a);
} else if (delta > 0) {
    x1 = (-b + Math.SQRT(delta))/ (2*a);
    x2 = (-b - Math.SQRT(delta))/ (2*a);
    console.log ("this question has two solutions [x1,x2]")
} else {
    console.log ("No solution")
}